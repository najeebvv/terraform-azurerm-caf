role_mapping = {
  built_in_role_mapping = {
    storage_accounts = {
      crowdstrike = {
        "Storage Blob Data Contributor" = {
          object_ids = {
            keys = [""] # to generate access token.
          }
        }
      }
    }
  }
}
